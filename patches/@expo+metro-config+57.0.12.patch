diff --git a/node_modules/@expo/metro-config/build/serializer/serializeChunks.js b/node_modules/@expo/metro-config/build/serializer/serializeChunks.js
index 10c8ce3..ec44b12 100644
--- a/node_modules/@expo/metro-config/build/serializer/serializeChunks.js
+++ b/node_modules/@expo/metro-config/build/serializer/serializeChunks.js
@@ -519,6 +519,9 @@ function gatherChunks(runtimePremodules, chunks, settings, preModules, graph, op
             else if (asyncType &&
                 // Workers require standalone bundles even when ordinary chunk splitting is disabled.
                 (isWorker || splitChunks)) {
+                if (isWorker && options.includeAsyncPaths) {
+                    continue;
+                }
                 const asyncChunks = gatherChunks(runtimePremodules, chunks, { test: pathToRegex(dependency.absolutePath) }, isWorker ? runtimePremodules : [], graph, options, true, isWorker);
                 // Seal all chunks that are for web workers, as these must be self-sufficient chunks
                 if (isWorker) {
